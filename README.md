# Enhanced Image Classification Using CNN

This project demonstrates an improved Convolutional Neural Network (CNN) for image classification, integrating batch normalization, dropout, and residual connections to boost accuracy and generalization. Tested on datasets like CIFAR-10, the enhanced model outperforms baseline CNNs with higher robustness against overfitting.
